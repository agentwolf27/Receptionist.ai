# SQLite migration history (archived)

These folders were generated when the datasource used SQLite. The app now uses PostgreSQL; new migrations live under `prisma/migrations/`. Keep this archive only if you need to diff old SQLite SQL.
